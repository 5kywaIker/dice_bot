High Prio:
Mehrfach-modifier sollen im output-string zusammengezogen werden (z.B. [8] + 4 + 2 sollte [8] + 6 sein
subembedded excecutable commands, wie z.B. /new damageplus change[str, show{str}+1]
    show soll vor change ausgeführt werden.
math command um bei change[get{}+1] nicht mit 6+1-1+1-1+1 zu enden, sondern die zahl zusammenzurechnen (could be put directly into change_command)
ad[di[2]] sollte sich gegenseitig auscanceln, ad[ad[]] sollte alle bis auf ein ad verschwinden lassen.
    benötigt Extra Behandlung in call_custom_command

Mid Prio:
casing ignorieren bei eingaben
"match_substring" soll auch die abfrage behandeln ob die resultierende Liste leer ist oder nicht und das in einer Variable zurückgeben.
-proficiency_command um einem attribut proficiency hinzuzufügen, oder zu entfernen
lower bound bei 1 bei würfen
vielleicht sechs extra attribut modifier einbauen, um zwischen Strenght (zu würfeln) und Strenght (referenzwert für attribute) zu unterscheiden. Name suggestion: "strevalue" o.ä.
    könnte in change_command eine abfrage einbauen, die automatisch strength mit strengthvalue austauscht, dass der Spieler sich keine gedanken machen muss.
    problem: in Alice' Fall, wo -r rage den str (zu würfeln) auf adv setzen soll, würde rage change auführen und dann strengthvalue ändern
    weiteres problem: prioritisierung. Wenn wir wollen, dass die Attribute auf strevalue zugreifen, müssen wir entweder in jedem Attribut strengthvalue ausschreiben, oder strevalue vor strength packen.
        aber dann würde -r stre strengthvalue aufrufen, und nicht strenght.

Low Prio:
read und write aktionen in eine eigene funktion schreiben. Aktuell führen 6 versch Funktionen fast genau den gleichen Code zum schreiben und lesen aus.
dm_command
hidden rolls
longrest und shortrest implementieren
different charactersheets for the same player, switchable
setall
hp tracken
Textausgabe bei nat20 o. nat1

Done:
call custom command muss eine variable anzahl an Parametern übergeben können, da verschiedene commands verschiedene anzahl an param nehmen (mit *args).
nuke and rewrite call_custom_command. Funktioniert nicht richtig und ist fully janky.
attribute calls im roll channel, ohne -r davor sollen trotzdem ausgeführt werden. [@bot.event() on_message dafür nutzen.] Error handling und CommandNotFound dafür genutzt.
Ausgabe korrekt kompilieren, aktuell wird bei attributwurf der modifier und nicht das attribut vor dem : angezeigt.
rewrite params, player_id should always be second
bei exception handling übergeben welches attribut z.B. nicht eindeutig war
aus bot_functions alle funktionen die keine commands für dice_bot sind in ein anderes file auslagern
Attribute umschreiben, dass sie die Fähigkeitswerte und den Proficiencybonus referenzieren -> Proficiency Bonus zu attribute_liste hinzufügen
exception handling aufräumen
@commands.check(roll_channel)
async with ctx.channel.typing()
match_substring erweitern um code zu entlasten
savingthrows
advantage und disadvantage (by default nur auf den ersten würfel)
attack roll attr erstellen
display stats command
Mehrere Würfe in einem Befehl, die nicht zusammenaddiert werden. Trennung möglicherweise mit | oder " ".
attack roll attr aufteilen in tohit und damage
tohit in player_attribute.txt
advantage rolls würfeln aktuell nur auf den ersten d20 roll adv -> sollen auf den ersten d20 wurf und auf alle nachfolgenden attribut-würfe adv geben, falls "-ad dext|str"
aufräumen: roll_standard, sowie roll_attribute und roll_dice sollten in ein eigenes file
spellslot system implementieren -> dafür zweiten command array in player_spell_list, soll mit -create_custom moonbeam 2d10 1d10 2 gesettet werden können und mit -s moo(nbeam) 2 gewürfelt werden können
change_command() muss auch custom commands ändern können
selbst commands programmieren können, bzw attribute (.append)
getall
-s_command(ctx, spellname, spelllevel=0)
bei interner attributabfrage wird geguckt, ob der eingegebene substring im attributnamen enthalten ist. Ändern, dass er nur guckt ob es damit anfängt
delete_command erstellen
print_command
